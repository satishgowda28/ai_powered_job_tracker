# AI-Powered Job Tracker — Backend

## Run locally

1. Copy `.env.example` to `.env` and fill values.
2. Install deps: `go mod tidy`
3. Run: `go run ./cmd/server`
4. Health: GET <http://localhost:8080/health>
